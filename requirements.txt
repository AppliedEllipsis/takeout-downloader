# Core
requests>=2.28.0
python-dotenv>=1.0.0

# TUI (the only interface)
textual>=0.50.0
rich>=13.0.0

# Web UI
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# For building a single binary
pyinstaller>=6.0.0
